# Crash-Consensus Dynamic Library

## Requirements

- [conan](https://conan.io/) package manager

## Build

From within root:

```sh
./build.sh crash-consensus
cd crash-consensus/shared-lib && ./build.sh
```

Headers are in `./build/include`, while the lib is in `./build/lib`.
